import HeroSection from '@/components/home/HeroSection';

const HomePage = () => {
  return (
    <section className="container flex flex-1 h-full">
      <HeroSection />
    </section>
  );
};

export default HomePage;
